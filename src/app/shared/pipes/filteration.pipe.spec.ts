import { FilterationPipe } from './filteration.pipe';

describe('FilterationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterationPipe();
    expect(pipe).toBeTruthy();
  });
});
